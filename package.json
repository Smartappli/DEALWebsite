{
  "name": "dealiot-website",
  "private": true,
  "version": "0.0.0",
  "description": "Static public website for the DEAL suite.",
  "scripts": {
    "build": "tailwindcss -i ./src/styles.css -o ./styles.css --minify",
    "watch": "tailwindcss -i ./src/styles.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}
